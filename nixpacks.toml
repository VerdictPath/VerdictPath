[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "cd backend && npm install --production=false",
  "npm install",
  "npm run build:web || true"
]

[start]
cmd = "cd backend && npm start"
