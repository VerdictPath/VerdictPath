# Railway Configuration for Verdict Path Backend
# This deploys ONLY the backend Node.js/Express server

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd backend && npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Watch only backend directory to prevent unnecessary rebuilds
watchPaths = ["/backend/**"]
